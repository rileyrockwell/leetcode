# Definition for singly-linked list.
class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next


class Solution:
    def deleteDuplicates(self, head):
        # riley, if you are blessed enough to be behind a computer, you must exert yourself
        # in the same capacity as the laborer.

        # riley: self-compassion. think about it, in your down time.
        return 1


head = ListNode(0)

instance = Solution().deleteDuplicates(head)
print(instance)