### TWO POINTERS ###


def is_subsequence(s, t):
	# use two pointers


def combine(arr1, arr2):
	# use two pointers


def check_if_target(arr, target):
	# use two pointers


def is_palindrome(s):
	# use two pointers
	left = 0
	right = len(s) - 1

	while left < right:
		if s[left] != s[right]:
			return False

		left += 1
		right -= 1

	return True



# time complexity


# space complexity


### SLIDING WINDOW ###

def find_length(nums, k):
	# find length of what?
	pass